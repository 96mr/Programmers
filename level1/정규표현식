class Solution {
    public boolean solution(String s) {
        boolean answer = true;
    
        if(s.length() == 4 || s.length() == 6){ //4 or 6글자
          return s.matches("(^[0-9]*$)"); //숫자로만 구성
        }else{
          return false;
        }
    }
}
